# Projet-CTF-Formatif

##To start the CTFd docker

Make sure that the docker service is up:
`service --status-all`

If not running:
`service docker start`

Then run the docker:
`# docker-compose up`

